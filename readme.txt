The number inserted by the user will be checked  for:

To do this the user can enter a valid roman to decimal number and
by calling the relevant function through the url provided in the manual get the
equivalent on the other format.

The application has also been implemented in the way that it will validate the input
before executing the function on it.


To get information on how to use tha application and its functionality use the link: /info

